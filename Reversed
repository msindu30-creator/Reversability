#include <stdio.h>
#include <string.h>

int main()
{
    char word[50];
    int i;

    printf("Enter a word: ");
    scanf("%s", word);

    printf("Reversed word: ");
    for (i = strlen(word) - 1; i >= 0; i--)
    {
        printf("%c", word[i]);
    }

    printf("\n");

    return 0;
}
